{
  "name": "cf-cocominevpn-api",
  "version": "1.0.1",
  "scripts": {
    "dev": "yarn run wrangler dev src/index.ts --env dev",
    "deploy": "yarn run wrangler deploy src/index.ts --minify"
  },
  "dependencies": {
    "hono": "^4.11.9"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240403.0",
    "@types/node": "^20.12.7",
    "wrangler": "4"
  }
}
